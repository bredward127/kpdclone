# FAL model documentation review

Reviewed 2026-08-27: [GPT Image 1.5 API reference](https://fal.ai/docs/model-api-reference/image-generation-api/gpt-image-1.5).

Documented values used by the registry:

- Endpoint ID: `fal-ai/gpt-image-1.5`
- Endpoint URL: `https://fal.run/fal-ai/gpt-image-1.5`
- Required input: `prompt` (string)
- Documented input fields: `image_size`, `background`, `quality`, `num_images`, `output_format`, and `sync_mode`
- Documented image sizes/aspect-ratio choices: `1024x1024`, `1536x1024`, and `1024x1536`
- Provider pricing metadata: not copied into this registry because the reviewed page excerpt does not supply a stable pricing value; the registry exposes a nullable display field instead.

The entry remains inactive by default. Activation must be an explicit administrator/configuration decision after re-reviewing the current official page. No model behavior is inferred from memory.
